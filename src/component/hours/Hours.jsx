 

 import PropTypes from 'prop-types'

const Hours = ({handelerselecetd}) => {
    
    return (
        <div>
            <h2 className="text-2xl">Total credit Hours : 0</h2>
        </div>
    );
};
Hours.propTypes={
 handelerselecetd:PropTypes.func
}

export default Hours;